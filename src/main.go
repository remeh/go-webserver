package main;

import (
    "fmt"
    "./core"
);

// ---------------------- 

func main() {
    // instanciate the app
    var app core.App;
    app.Init();

    // reads the main configuration
    fmt.Println("--");
}

